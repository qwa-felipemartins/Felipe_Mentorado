package br.com.qwasolucoes.mentoria.implementacoes.algoritmos;

public class Vetor {

	/*public void adiciona( int posicao, String elemento){
	if(!(posicao >= 0 && posicao <=tamanho)){
	throew new IllegalArgumentExceprion("Posicao Invalida");
	}
	for (int i = 0 tamanho - 1; i >= posicao -1 ; i--){
	elementos[i+1] = elementnos[i];
	}
	elementos[posicao] = elemento;
	tamanho++;
	}
	private void aumentaCapacidade(){
	if(this.tamanho == this.elementos.length){
	
	String[] elementosNovos = new String[this.elementos.length * 2];
	for(int i = 0 ; i < this.elementos.length; i++){
	elementosNovos[i] = this.elementos[i];
	}
	this.elemento= elementosNovos;
	}
	
	
	public void remover(int posicao){
	if(!posicao >= 0 && posicao < tamanho )){
	
	throew new IlegalArgumentException("Posicao Invalida");
	}
	for(int i = posicao; i < this.tamanho -1;i++){
	this.elementos[i] = this.elementos[i+1];
	}
	this.tamanho --;[
	}
	
	
	
	
	
	
	
	}
	
	
	*/
}
